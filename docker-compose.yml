version: "3.7"

services:
  php:
    image: php:7.4-fpm-alpine
    working_dir: /var/www
    volumes:
      - .:/var/www
    ports:
      - "8000:8000"
    command: "php artisan serve --host=0.0.0.0 --port=8000"